# 설계 결정 기록 (Decision Log)

이 프로젝트를 진행하며 내린 설계·구현 결정을 시간 순으로 기록합니다.
목적은 하나입니다 — 나중에 "왜 이렇게 만들었어요?"라는 질문에 코드가 아니라
**이 문서를 펴서** 답할 수 있게 하는 것.

각 항목은 이 순서로 씁니다:
- **상황**: 뭘 정해야 했는가
- **검토한 대안**: 후보로 고려한 것들 (기각한 것도 왜 기각했는지 포함)
- **결정**: 최종 선택
- **트레이드오프**: 이 선택으로 포기한 것 / 감수하는 리스크

---

<!-- 아래부터 실제 결정이 하나씩 쌓입니다. -->

## 2026-08-09 — 검증 게이트 구현 방식: 직접 구현 vs 프레임워크 vs 하이브리드

**상황**
데이터 품질 검증 게이트를 어떻게 만들지 정해야 했다. 세 가지 방식을 두고 고민:
1. 순수 Python/pandas로 전부 직접 구현
2. Pandera·Great Expectations 같은 기존 데이터 검증 프레임워크 활용
3. 스키마/타입 체크는 기존 툴, 통계적 분포 이동 감지와 건강데이터
   도메인 규칙(예: 수축기≥이완기 등)은 직접 구현하는 하이브리드

**검토한 대안과 기각 이유**
- 순수 직접 구현: 모든 코드를 스스로 설명할 수 있다는 장점은 있지만,
  스키마·타입 체크처럼 이미 잘 만들어진 도구가 있는 영역까지 바퀴를
  재발명하는 데 시간을 쓰는 건 비효율적이라 판단해 기각.
- 순수 프레임워크 활용: 실무 툴 경험은 어필되지만, 이 프로젝트의
  핵심 차별점이 되어야 할 통계적 판단(분포 이동 감지 기준을 어디로
  잡을지 등)까지 프레임워크 뒤에 숨어버려서 "직접 설계한 부분"이
  약해질 위험이 있어 기각.

**결정**
하이브리드로 간다. 스키마/타입/범위처럼 정답이 명확한 체크는 기존
프레임워크에 맡기고, 통계적 분포 이동 감지와 도메인 특화 규칙처럼
"어디에 기준선을 그을지" 판단이 필요한 부분은 직접 구현한다.

**트레이드오프**
초반에 결정할 게 더 많아지고(어떤 프레임워크를 쓸지, 툴과 직접구현의
경계를 어디로 그을지) 코드베이스도 두 스타일이 섞인다. 대신 "실무
툴을 쓸 줄 알면서 동시에 핵심 로직은 스스로 설계할 수 있다"는 걸
같이 보여줄 수 있다.

---

## 2026-08-09 — 스키마/타입 체크 라이브러리: Pandera

**상황**
하이브리드 방식 중 "기존 프레임워크" 파트를 뭘로 채울지 정해야 했다.
후보: Pandera / Great Expectations / Pydantic.

**검토한 대안과 기각 이유**
- Great Expectations: 실무 인지도는 높지만, expectation suite·체크포인트
  설정 자체의 러닝커브가 있어 "여유있게 몇 주"라는 일정 중 상당 부분을
  이 프로젝트의 핵심이 아닌 툴 설정에 쓰게 될 위험이 있다고 판단해 기각.
- Pydantic: row(레코드) 단위 강타입 검증에는 강하지만, 이 프로젝트가
  다루는 건 DataFrame 전체의 통계적 성질(분포 이동 등)이라 태생적으로
  안 맞는다. row마다 개별 검증하면 대용량에서 느려질 수 있어 나중에
  불필요한 성능 이슈를 만들 수 있다고 보고 기각.

**결정**
Pandera. pandas DataFrame에 스키마를 바로 선언해서 붙일 수 있고,
배우는 데 오래 걸리지 않아 이 프로젝트의 진짜 차별점인 통계적 분포
이동 감지·도메인 규칙 쪽에 시간을 더 쓸 수 있다.

**트레이드오프**
Great Expectations만큼의 "이름값"이나 Data Docs 같은 자동 리포트
기능은 포기한다. 필요하면 나중에 검증 결과를 별도로 리포트화하는
로직을 직접 짜야 한다.

---

## 2026-08-09 — 첫 마일스톤 범위: 스키마/타입 체크부터

**상황**
검증 게이트를 이루는 세 체크(스키마/타입, 통계적 분포 이동, 도메인
규칙) 중 뭘 먼저 구현할지 정해야 했다.

**검토한 대안**
- 분포 이동 감지부터: 가장 어렵고 차별화되는 부분을 먼저 검증해
  기술 리스크를 일찍 없애는 방식(riskiest-first). 다만 초반 진도가
  더디게 느껴질 수 있음.
- 도메인 규칙부터: 가장 빨리 끝나지만 로직이 단순해 임팩트가 작음.

**결정**
스키마/타입 체크부터. 엔드투엔드 파이프라인의 뼈대(신규 데이터 →
검증 → 통과/실패 리포트)를 먼저 돌아가게 만들고, 그 위에 통계적
분포 이동 감지·도메인 규칙을 순서대로 얹기로 함.

**트레이드오프**
이 프로젝트의 가장 차별화된 부분(분포 이동 감지)의 기술 리스크
검증이 뒤로 밀린다. 만약 나중에 그 부분에서 설계가 꼬이면 이미
짜둔 스키마 체크 코드와 인터페이스를 다시 맞춰야 할 수 있음.

---

## 2026-08-09 — 스키마 범위 기준: 데이터 관측값이 아니라 도메인 지식

**상황**
각 컬럼의 유효 범위(min/max)를 뭘 기준으로 잡을지 정해야 했다. 손에 있는
학습 데이터(`train_dataset.csv`)의 실제 min/max를 그대로 스키마 범위로
쓸 수도 있었다.

**검토한 대안**
- 학습 데이터 관측 범위를 그대로 사용: 빠르고 구현이 간단하지만, 학습
  데이터에 우연히 없었던 값(예: 96세)까지 전부 "무효"로 거부하게 되는
  문제가 있다. 검증 게이트가 아니라 "이 특정 CSV 재현기"가 되어버림.

**결정**
의학적으로 있을 법한 범위(도메인 지식)를 기준으로 상/하한을 잡는다.
정상 범위보다는 넓게, 그러나 물리적으로 불가능한 값은 걸러지도록.

**트레이드오프 + 실측 결과**
이 기준으로 실제 학습 데이터 38,984건을 돌려본 결과, 86건(88개 이슈)이
실패했다 — Gtp 37건, ALT 26건, LDL 22건, AST 2건, HDL 1건. 즉 이 스키마는
학습 데이터 자체에 있던 극단값도 "정상 범위 밖"으로 잡아낸다. 이건
버그가 아니라 의도한 동작이지만, 이 스키마를 그대로 학습 데이터 전처리에
쓰면 원래 팀 프로젝트가 만들었던 "전처리 후 38,740건"과는 다른 숫자가
나온다는 뜻이기도 하다. 두 기준(도메인 기준 vs 팀이 썼던 단순 임계값)이
다르다는 걸 인지하고 있어야 한다.

---

## 2026-08-09 — 시력(eyesight) 필드: 9.9는 이상치가 아니라 특수 코드

**상황**
시력 컬럼의 값 범위를 정하다가, 실제 데이터에 0.1~2.5 사이의 "정상적인"
값들과 별개로 9.9라는 값이 섞여 있는 걸 발견했다.

**검토한 대안**
- 단순 범위 체크(예: 0.1~2.5)만 적용: 구현은 제일 간단하지만, 9.9는
  이 데이터셋에서 "실명 또는 측정 불가"를 뜻하는 정상적인 특수 코드다.
  단순 범위 체크를 쓰면 이 유효한 값이 전부 "이상치"로 잘못 거부된다.
  실제로 원래 팀 프로젝트(`smoking-viz-project`)에서 이 문제를 모른 채
  진행되어, 팀원마다 다른 임계값(eyesight ≤ 2.0 / ≤ 3.0)으로 이 값을
  걸러냈었다 (`docs/03_issues_and_troubleshooting.md` Issue 1 참고) — 그
  임계값 차이가 정확히 이 9.9 코드를 걸렀는지 여부에서 갈렸을 가능성이
  높다.

**결정**
"0.1~2.5 범위 또는 정확히 9.9" 를 유효한 값으로 명시적으로 허용하는
커스텀 체크(`@pa.check`)를 작성했다. 9.9는 더 이상 이상치가 아니라
"실명"이라는 별도의 유효한 카테고리로 취급된다.

**트레이드오프**
이 스키마 통과 여부만으로는 "정상 시력"과 "실명"을 구분할 수 없다
(둘 다 통과). 이후 모델링 단계에서 9.9를 실제로 어떻게 다룰지(별도
피처로 인코딩할지, 결측으로 취급할지)는 아직 정하지 않은 별개의 결정.

---

## 2026-08-09 — 구현 중 발견: Pandera class-based 모델의 커스텀 체크 문법

**상황**
`pa.Field(checks=pa.Check(lambda ...))` 형태로 시력 필드에 커스텀 체크를
넣으려다 `SchemaInitError: custom check 'checks' is not available` 에러가
났다. class-based `DataFrameModel`에서는 `Field(checks=...)`가 지원되지
않고, `@pa.check("컬럼명")` 데코레이터로 별도 메서드를 정의해야 했다.

**결정**
`@pa.check("eyesight(left)", "eyesight(right)")`로 데코레이트한 클래스
메서드로 교체. 실제 실행해서 실패 케이스가 의도대로 잡히는지 확인 후
반영.

**비고**
사소해 보이지만 기록해두는 이유: "라이브러리 문서만 보고 짠 코드가
처음부터 한 번에 돌아가지 않았다 → 실제로 실행해보고 에러 메시지 보고
고쳤다"는 흔적이라, 이런 것도 남겨두는 게 낫다고 판단함.

---

## 2026-08-09 — 코드 리뷰: 간수치/LDL 극단값, 하드 실패 유지로 확정

**상황**
스키마 리뷰 중, AST/ALT/Gtp/LDL 상한을 넘는 값(예: 급성 간염 환자의
AST=수천)이 "드물지만 실제로 있을 수 있는 값"인데도 형식 오류와 똑같이
하드 실패 처리되는 게 맞는지 논의했다.

**검토한 대안**
- 경고(warning) 등급을 별도로 둬서, 극단값은 실패가 아니라 "검토 필요"로
  약하게 처리하는 방법도 있었다.

**결정**
지금 단계에서는 하드 실패를 유지한다. 이유: 이 게이트의 현재 역할은
"모델 학습/추론에 넣기 전 최소한의 안전장치"이고, 이 단계에서 애매한
값까지 통과시키는 것보다는 일단 다 걸러내고 사람이 확인하게 하는 편이
안전하다고 판단. "정말 드물지만 유효한 값"과 "데이터 오류"를 구분하는
문제는 이 프로젝트의 범위를 넘어서는 임상적 판단이 필요한 영역이라
지금 다루지 않기로 함.

**트레이드오프**
드문 실제 임상 케이스가 있는 신규 데이터가 들어오면, 그 배치 전체가
스키마 검증에 걸려 모델에 도달하지 못한다. 실제 서비스라면 이건
"오탐(false positive)으로 인한 가용성 손실" 리스크다. 지금은 정확성을
우선하고 이 리스크는 알고 있는 상태로 감수하기로 함.

---

## 2026-08-09 — 코드 리뷰: strict=True → 완화 (새 컬럼은 경고로)

**상황**
스키마에 없는 컬럼이 하나라도 섞이면 배치 전체가 실패하는
`strict=True` 설정을 리뷰했다. 이 프로젝트가 안고 있는 한계점 중 하나가
"성별 데이터 부재"였는데(`smoking-viz-project/docs/03` Issue 6),
`strict=True`인 채로는 나중에 성별 컬럼이 실제로 추가된 더 좋은
데이터가 들어와도 스키마에 없는 컬럼이라는 이유만으로 통째로 거부된다
— 데이터 품질을 지키려던 장치가 오히려 데이터 개선을 막는 상황.

**결정**
`Config.strict`를 `False`로 완화하고, 대신 `gate.py`에 별도 로직을 추가해
스키마에 없는 컬럼이 있으면 실패시키지 않고 `ValidationResult.warnings`에
담아 보고하도록 바꿨다. 알려진 컬럼에 대한 검증(범위·타입)은 그대로
유지되고, "새 컬럼이 있다"는 사실만 경고로 남는다.

**트레이드오프**
스키마에 없는 컬럼을 통해 의도치 않은 데이터(오타난 컬럼명, 잘못
합쳐진 데이터 등)가 조용히 섞여 들어올 위험이 생긴다. "경고"이므로
파이프라인이 이걸 놓치고 그냥 지나칠 수 있다 — 경고를 실제로 누가/언제
확인하는지는 아직 정하지 않았다 (지금은 로그에 찍히는 것으로 충분하다고
보고, 알림 체계는 이 프로젝트 범위 밖으로 뒀다).

---

## 2026-08-09 — 도메인 규칙 마일스톤: 아키텍처 분리 + 규칙 2개

**상황**
도메인 규칙(여러 컬럼을 같이 봐야 아는 문제)을 구현해야 했다. Pandera
스키마 클래스 안에 `@pa.dataframe_check`로 같이 넣을 수도 있었고, 완전히
별도 모듈로 뺄 수도 있었다.

**검토한 대안**
- Pandera 스키마에 통합: 에러 수집(lazy=True) 인프라를 그대로 재사용할
  수 있어 코드가 줄어든다. 다만 schema.py 맨 위에 "이 파일은 형식 체크만
  한다"고 못박아둔 것과 충돌한다 — 나중에 "이 파일이 뭘 책임지는지"
  설명할 때 애매해짐.

**결정**
`domain_rules.py`로 완전히 분리했다. `gate.py`가 두 결과(스키마 +
도메인 규칙)를 하나의 리포트로 합치는 조율자 역할을 한다. 첫 두 규칙은
① 수축기>이완기, ② 키·체중으로 계산한 BMI가 10~80 안에 있는가.

**실측 결과**
실제 학습 데이터 38,984건에 대해 두 규칙 다 위반 0건. 즉 이 데이터셋
자체는 관계형 오류가 없고, 지난 마일스톤에서 잡은 스키마 위반(86건)이
이 데이터의 실제 문제였다. 도메인 규칙은 지금 이 데이터에서는 "조용한
안전장치"지만, 향후 다른 소스에서 데이터가 들어올 때 진가를 발휘할
장치라고 봄 — 지금 안 잡힌다고 무의미한 코드는 아니라고 판단.

**트레이드오프**
아키텍처는 깔끔해졌지만 파일이 두 개로 늘어 진입점이 하나가 아니게
됐다(`gate.py`가 이 둘을 알아야 함 → 결합도 존재). 규칙이 늘어날수록
`ALL_RULES` 리스트에 등록하는 걸 잊는 실수가 날 수 있음 — 이건 나중에
"규칙 자동 등록/레지스트리 패턴"으로 개선할 수 있는 지점으로 남겨둠.

---

## 2026-08-09 — 분포 이동 감지: PSI 주력 + KS-test 보조, 왜 그렇게 나눴는가

**상황**
드리프트(새 배치의 분포가 기준과 달라졌는가)를 어떻게 감지할지 정해야
했다. 이 프로젝트 데이터는 3만 8천여 건으로 표본이 크다는 게 이미
smoking-viz-project 상관관계 분석 때부터 반복해서 문제였다 — 표본이
크면 아주 작은 차이도 통계적으로 "유의함"이 나온다.

**결정**
PSI(Population Stability Index)를 1차 판단 기준으로 쓴다. 업계에서
피처 드리프트 모니터링용으로 널리 쓰이고, 표본 크기에 상대적으로 덜
휘둘리는 해석 가능한 임계값(0.1=안정, 0.25=심각)을 제공하기 때문.
KS-test(p-value)는 참고용 보조 지표로만 같이 보여준다 — 단독으로
pass/fail을 가르는 데는 안 쓴다.

**스코프에서 의도적으로 뺀 것**
이진/범주형 컬럼(hearing, Urine protein, dental caries, smoking)은
분위수 기반 구간 나누기가 안 맞아서 제외했다. 시력(eyesight)도 제외했다
— 9.9(실명 코드)가 0.1~2.5 정상값과 섞여 있어 분위수 구간이 심하게
왜곡되기 때문. 카테고리별 비율 비교라는 별도 방식이 필요한데, 이번
마일스톤 범위 밖이라 다음 개선 과제로 남긴다.

**실측 검증 — 그리고 예상 밖의 발견**
`scripts/demo_drift_smoker_vs_nonsmoker.py`로 비흡연자를 "기준",
흡연자를 "신규 배치"로 놓고 실제 데이터에 돌려봤다. 상위권(hemoglobin,
Gtp, triglyceride, serum creatinine, waist)이 예전 상관관계 분석의
핵심 변수와 거의 겹쳐서, 서로 다른 두 통계 방법이 같은 결론에
도달했다 — 이건 이 드리프트 감지기가 실제로 의미 있는 신호를 잡아낸다는
교차검증이다.

그런데 1위가 `height(cm)`(PSI=1.05)로 나왔다. 키가 흡연 때문에 변할 리는
없다. 이건 흡연율의 성별 불균형에 의한 **혼입(confounding)** 신호로
보인다 — smoking-viz-project에서 계속 "한계점"으로만 언급됐던 성별
데이터 부재 문제가, 여기서는 감이 아니라 **정량적 수치로** 드러난
셈이다. 이 드리프트 감지기가 원래 목적(전처리 이후 배치 모니터링)
말고도, 기존 분석의 숨은 편향을 찾는 데도 쓰일 수 있다는 뜻이라
기록해둔다.

**트레이드오프**
드리프트는 "이 행이 틀렸다"가 아니라 "이 배치 전체가 통계적으로
달라 보인다"는 population 수준 신호라, 스키마·도메인 규칙과 달리
하드 실패가 아니라 경고로 다루기로 했다(다음 항목에서 gate.py 통합
시 반영). 이 스크립트는 원본 CSV가 저장소에 없어 pytest 스위트가
아니라 수동 실행 데모로만 남긴다 — CI에서 자동으로 도는 검증은 아니다.

---

## 2026-08-09 — gate.py 통합: validate_batch와 드리프트를 분리 (B안)

**상황**
`check_drift()`를 `validate_batch()`에 합칠지(선택적 `reference` 인자
추가), 완전히 별도 함수로 뺄지 결정해야 했다.

**결정**
별도 함수로 분리(B안). `validate_batch(df)`는 그대로 두고,
`check_drift(reference, incoming) -> DriftReport`를 새로 추가했다.
`DriftReport.as_warnings()`로 두 결과를 호출부에서 필요할 때 합칠 수 있게
했다 — 결합은 강제하지 않고 선택할 수 있게.

**이유**
드리프트 체크는 "기준 분포"라는, 스키마·도메인 규칙에는 없는 입력이
필요하다. 이걸 `validate_batch`의 선택적 인자로 숨기면 함수 하나가
"배치 하나만 검증"과 "배치 두 개를 비교"라는 서로 다른 두 가지 일을
하게 된다. 함수 시그니처만 보고 "이 함수가 뭘 하는지" 알 수 있게
하는 걸 우선했다.

**검증**
`test_validate_batch_is_unaffected_by_drift_module` 테스트로,
`validate_batch`의 동작과 반환 타입이 드리프트 모듈 추가 전후로
전혀 안 바뀌었음을 확인했다. 전체 테스트 21개 통과.

**트레이드오프**
지금은 두 함수를 조합해서 "①검증 게이트"를 완성하는 코드가 없다 —
그건 실제로 이 게이트를 파이프라인에 붙이는 다음 단계(②예측 모델과
연결할 때)에서 만들기로 미뤘다. 지금 상태로는 "따로 쓸 수 있는 두
도구"이지 "하나의 완결된 게이트"는 아니다.

---

## 2026-08-10 — ②예측 모델: 버전 1개만, 성능 우선

**상황**
모델을 몇 개 만들지 정해야 했다. v1(로지스틱)+v2(GBM)을 지금 둘 다
만들어 다음 마일스톤(③버전 비교)을 위한 재료를 미리 준비해두는 방법과,
지금은 하나만 만들고 두 번째 버전은 실제로 ③에 도달했을 때 만드는
방법을 검토했다.

**결정**
하나만, 성능이 좋은 모델로. 이유를 명시적으로 묻지는 않았지만, 지금
단계에서 미래 마일스톤을 위해 코드를 미리 만들어두는 것보다 "지금
필요한 것"에 집중하는 게 낫다는 판단으로 이해하고 진행함.

---

## 2026-08-10 — 모델 알고리즘: HistGradientBoostingClassifier (scikit-learn)

**상황**
"성능 좋은 모델"이 구체적으로 뭘 뜻하는지는 위임받았다. XGBoost,
LightGBM, scikit-learn 내장 Gradient Boosting 계열을 검토.

**결정**
`sklearn.ensemble.HistGradientBoostingClassifier`. 이유:
1) XGBoost/LightGBM처럼 별도 무거운 의존성을 추가하지 않아도 된다
   (이미 쓰고 있는 scikit-learn 생태계 안에서 해결).
2) 결측값(NaN)을 대체값으로 채우지 않고 네이티브로 처리한다 — 시력
   9.9(실명) 처리 방식(아래 항목)과 맞아떨어진다.
3) 3만 8천여 건 규모에서 학습 속도·성능 모두 무난하다.

**남겨둔 리스크**
XGBoost/LightGBM 대비 실제 성능 차이를 직접 비교하지는 않았다.
"업계 표준"이라는 인지도 면에서는 XGBoost가 더 흔히 언급되므로,
면접에서 "왜 XGBoost 안 썼어요?"라는 질문에는 이 트레이드오프(의존성
최소화 vs 인지도)로 답할 수 있어야 함.

---

## 2026-08-10 — 피처: 시력 9.9(실명 코드)를 이진 플래그 + 결측으로 분리

**상황**
`schema.py`에서 "9.9는 이상치가 아니라 유효한 실명 코드"라고 이미
처리해뒀지만(스키마 통과), 모델 입력으로 그대로 넣으면 다른 문제가
생긴다 — 9.9라는 숫자가 커서, 모델이 "시력이 매우 좋다"는 반대 신호로
잘못 학습할 위험이 있다.

**결정**
`eyesight_left_blind`/`eyesight_right_blind` 이진 피처를 추가하고,
실명인 경우 원래 시력 값은 NaN으로 바꿨다. HistGradientBoostingClassifier가
NaN을 네이티브로 처리하므로 별도 대체(imputation)가 필요 없다.

**비고**
이 결정은 스키마 마일스�트에서 발견했던 문제(Issue: eyesight 9.9)를
피처 엔지니어링 단계까지 일관되게 끌고 온 것이다 — "발견했으면
끝까지 처리한다"는 걸 보여주는 사례로 남겨둔다.

---

## 2026-08-10 — 실측 결과: 게이트 연결 + 모델 성능

**상황**
`scripts/train_model.py`로 원본 CSV → 검증 게이트 → 게이트 통과 데이터만
학습, 전체 파이프라인을 실제로 돌려봤다.

**결과**
- 게이트: 38,984건 중 86건 제외, 38,898건으로 학습 (Issue 1에서 이미
  확인했던 그 86건과 동일).
- 성능(held-out 20%, n=7,780): Accuracy 0.753, Precision 0.659,
  Recall 0.676, F1 0.667, ROC-AUC 0.836.
- 다수 클래스(비흡연 63.3%)만 예측하는 베이스라인 대비 accuracy가
  12%p 높다 — 그냥 다수 클래스를 찍은 게 아니라 실제로 신호를
  학습했다는 근거.

**의의**
이게 이 프로젝트가 원래 하려던 것 — "검증 게이트를 통과한 데이터로만
모델을 학습한다"는 문장이 README에만 있는 게 아니라 실제로 코드에서
일어난다는 걸 증명한 지점. `filter_to_valid_rows()`가 없었다면 이
연결 자체가 존재하지 않았을 것.

---

## 2026-08-10 — 드리프트 체크는 왜 train_model.py에 안 넣었는가

**상황**
`scripts/train_model.py`를 만들면서, `check_drift()`도 같이 넣을지
고민했다.

**결정**
안 넣었다. 이유: 드리프트는 "기준 분포 대비 신규 배치가 얼마나
다른가"를 재는 것인데, **지금 이 스크립트가 하는 일 자체가 최초 학습**이라
비교할 "기준"이 아직 없다. 드리프트 체크는 학습 시점이 아니라, 학습
끝난 모델에 **새 데이터가 들어와 추론(scoring)할 때** 그 데이터가
학습 당시 분포와 다른지 보는 용도다. 즉 지금 만든 학습 스크립트와
드리프트 체크는 파이프라인상 서로 다른 시점에 쓰이는 도구라, 억지로
같이 묶으면 오히려 개념이 헷갈린다.

**앞으로 필요한 것**
③(버전 비교 감사기)이나 별도의 "추론 스크립트"를 만들 때, 이번에 학습에
쓴 데이터(38,898건)를 `check_drift()`의 기준(reference)으로 저장해두고,
새 배치가 들어올 때마다 그것과 비교하는 구조가 되어야 한다. 지금은 이
기준 데이터를 별도로 저장해두지 않았다 — 다음에 실제로 필요해질 때
만들기로 함(미리 만들지 않기로 한 것도 이번 세션의 결정과 일관됨).

---

## 2026-08-10 — ③버전 비교: v2 = v1 + WHtR (피처 차이로 감사기 테스트)

**상황**
②에서 미뤄뒀던 "두 번째 버전"을 이제 만들어야 했다. A(로지스틱 회귀,
성능 vs 해석력), B(피처 차이), C(하이퍼파라미터 차이) 중 B를 선택.

**결정**
v1 = 기존 피처 세트 그대로. v2 = v1 + WHtR(허리둘레/키 비율) 하나만
추가. WHtR을 고른 이유: waist(cm)이 이미 raw 피처로 들어가 있어서
WHtR이 진짜 새 정보인지 중복인지 결과가 뻔하지 않다 — 일부러 애매한
비교 대상을 골랐다.

**구현상 핵심 전제조건**
McNemar 검정은 두 모델이 "정확히 같은 테스트 샘플"에 낸 예측을
비교해야 성립한다. `train_model()`에 `feature_columns` 파라미터를
추가해서, 같은 `df`·같은 `random_state`로 v1/v2를 각각 학습해도
`train_test_split`의 행 분할(test_index)이 완전히 동일하게 나오는지
`test_v1_and_v2_get_identical_test_split_when_same_random_state`로
직접 확인했다 — 이게 깨지면 뒤의 통계 검정 전체가 무의미해진다.

**실측 결과**
```
v1: Accuracy 0.7526, ROC-AUC 0.836
v2: Accuracy 0.7530, ROC-AUC 0.834  (+0.0004, 오히려 AUC는 소폭 하락)
McNemar: statistic=0.0119, p=0.9132 → 유의한 차이 없음
```
WHtR 추가는 실질적으로 아무 도움도 안 됐다 — 예상대로 waist(cm)과
중복 정보였던 것으로 보인다. **감사기가 "이 개선은 가짜다"를 정확히
잡아냈다**는 게 핵심이다. 만약 이 감사기 없이 accuracy 숫자(0.7526 vs
0.7530)만 봤다면 "아주 조금이지만 좋아졌다"고 잘못 결론 내렸을 수 있다.

**트레이드오프**
이번 비교는 discordant pair가 337개(167+170)로 충분히 많아서
저검정력 경고는 안 떴다. 만약 게이트 통과 데이터가 지금보다 훨씬
적었다면(예: 수백 건), 진짜 차이가 있어도 못 잡아낼 수 있다는 걸
`low_power_warning` 필드로 미리 대비해뒀다 — 이번엔 발동 안 했지만
코드 경로 자체는 소표본 테스트로 확인됨.

